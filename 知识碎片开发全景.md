# 知识碎片系统 — 开发全景

> 最后更新：2026-04-03（v8：右键新建知识碎片/临时看板/阅览模式/总览重设计）

---

## 一、系统概述

知识碎片系统是 IPM 的核心子模块之一，负责**捕获、存储、管理、关联**用户在项目工作中产生的各类零散知识——包括剪贴板文本、截图、手动创建的富文本笔记等。

整体目标是将原本散落在 JSON 文件中的碎片数据升级为 SQLite 统一存储，提供完整的 CRUD + 关联管理 + 富文本编辑 + 网页剪藏能力，并在 UI 层提供全新的知识管理页面。

---

## 二、技术架构

```
┌─────────────────────────────────────────────────────────┐
│                      Renderer (React)                   │
│                                                         │
│  KnowledgePanorama (知识库全景 — 跨项目中台)                │
│       ├── 总览视图：统计卡片 + 看板预览 + 最近新增 + 标签 + 项目 │
│       ├── 知识管理视图：类型/项目筛选 + 搜索 + 标签聚合 + 新建  │
│       ├── 学习分区入口（StudyKnowledgeList）                  │
│       ├── WabiBoardPage（全屏看板 — Header/Canvas/Footer）   │
│       │     ├── BoardCanvas（无限画布/多选/框选/键盘快捷键）     │
│       │     │     ├── ConnectionLayer（SVG 红绳贝塞尔连线）     │
│       │     │     ├── GroupFrame（可视分组区域框）               │
│       │     │     └── Minimap（右下角缩略图导航）               │
│       │     ├── BoardCard（侘寂风格卡片 + 标签胶囊 + 锁定态）   │
│       │     ├── CollectModal（收集已有碎片到看板）              │
│       │     ├── QuickCreateModal（在看板上新建碎片）           │
│       │     └── BgMenu（画布背景切换：方格/点阵/纯净/自定义色）  │
│       ├── 草稿创建弹窗（文本/笔记/网页/截图 → Study DB）     │
│       ├── 草稿归属弹窗（选择目标项目 → assignDraft）         │
│       └── KnowledgeItemCard + KnowledgeDetailPanel (复用)  │
│                                                         │
│  KnowledgePage (项目级入口，双模式)                        │
│       ├── 管理模式：KnowledgeToolbar + ItemCard grid      │
│       ├── 关联模式：FileTree + ConnectorLines + cards     │
│       ├── Webclip 创建弹窗（URL 输入 → 抓取 → AI 摘要）    │
│       ├── KnowledgeDetailPanel                           │
│       │        ├── NoteEditor (BlockNote)                │
│       │        └── Webclip 详情视图（URL + 摘要 + 正文 + 截图上传） │
│       └── KnowledgeItemCard (支持拖拽/选择/管理 双形态)    │
│                                                         │
│  TrayWidget (悬浮窗，已改造，使用 knowledge API)           │
│                                                         │
│  window.ipm.knowledge.*  (preload 暴露)                  │
├─────────────────────────────────────────────────────────┤
│                      IPC 层                              │
│                                                         │
│  知识碎片（单项目）:                                       │
│  knowledge/list · knowledge/get · knowledge/create      │
│  knowledge/update · knowledge/delete · knowledge/search │
│  knowledge/addLink · knowledge/removeLink               │
│  knowledge/getLinks · knowledge/getLinkedItems           │
│  knowledge/createWebclip · knowledge/addWebclipImage    │
│  knowledge/stats                                        │
│                                                         │
│  跨项目聚合（v4 新增）:                                    │
│  knowledge/listGlobal · knowledge/statsGlobal           │
│  knowledge/createDraft · knowledge/assignDraft           │
│  knowledge/listDrafts · knowledge/listProjects           │
│                                                         │
│  knowledge:changed (事件广播)                             │
├─────────────────────────────────────────────────────────┤
│                    Main Process (Node.js)                │
│                                                         │
│  src/main/ipc/knowledge.js  (IPC handler)               │
│  Agent/db/knowledge.js      (数据访问层)                  │
│  Agent/db/init.js           (Schema v3 迁移)             │
│  Agent/db/migrate.js        (JSON→SQLite 迁移)           │
│  Agent/services/webclip.js  (Readability 正文提取 + AI 摘要) │
│                                                         │
│  物理文件：                                               │
│    snippets/clipboard/*.txt   (文本碎片)                  │
│    snippets/screenshots/*.png (截图碎片 + webclip 截图)    │
│    笔记/webclip 碎片不写物理文件，正文存 DB                 │
├─────────────────────────────────────────────────────────┤
│                    SQLite (project.db)                   │
│                                                         │
│  knowledge_items (碎片主表)                               │
│  knowledge_links (碎片-文件关联表，多对多)                  │
└─────────────────────────────────────────────────────────┘
```

---

## 三、数据模型

### 3.1 knowledge_items 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT PK | 碎片唯一 ID，格式 `{type前4字}_yyyyMMdd_HHmmss_{shortId}` |
| type | TEXT | snippet / screenshot / note / webclip |
| title | TEXT | 标题 |
| content_text | TEXT | 纯文本内容（供搜索 + 预览） |
| content_json | TEXT | 富文本 JSON（BlockNote document，note 类型用） |
| content_path | TEXT | 关联的物理文件相对路径 |
| summary | TEXT | 摘要 |
| tags | TEXT | JSON 数组 `["tag1","tag2"]` |
| importance | TEXT | low / medium / high / null |
| source_kind | TEXT | clipboardText / clipboardImage / manual / webclip |
| source_url | TEXT | 来源 URL（webclip 用） |
| pinned | INTEGER | 0/1 置顶 |
| archived | INTEGER | 0/1 归档 |
| created_at | TEXT | ISO 时间戳 |
| updated_at | TEXT | ISO 时间戳 |

### 3.2 knowledge_links 表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| item_id | TEXT FK | 引用 knowledge_items(id)，CASCADE 删除 |
| target_path | TEXT | 关联的文件/文件夹相对路径 |
| target_kind | TEXT | file / dir |
| created_at | TEXT | ISO 时间戳 |

### 3.3 存储策略

- **每项目独立**：碎片表直接加入现有的 `project.db`（Schema v3），与 suggestions、events 等表共存
- **Study 空间**：study 空间同样有独立的 `project.db`，碎片存储在 study 的 DB 中，作为跨项目的全局知识空间
- **物理文件不变**：文本碎片仍写 `snippets/clipboard/*.txt`，截图仍写 `snippets/screenshots/*.png`
- **笔记类型不写物理文件**：type=note 的内容全部存在 `content_json`（BlockNote JSON）+ `content_text`（纯文本导出）

---

## 四、已完成的功能

### Phase 1: 数据库层 ✅

| 模块 | 文件 | 内容 |
|------|------|------|
| Schema v3 | `Agent/db/init.js` | CURRENT_VERSION=3，MIGRATION_V3 创建 knowledge_items + knowledge_links + 索引 |
| 数据访问层 | `Agent/db/knowledge.js` | createItem, getItem, listItems, updateItem, deleteItem, addLink, removeLink, removeLinkByItemAndPath, getItemLinks, getLinkedItems, countItems, countLinkedItems |
| JSON 迁移 | `Agent/db/migrate.js` | migrateSnippetsJsonToSqlite：迁移 clipboard-record.json + screenshots-record.json → SQLite |
| 迁移调用 | `Agent/db/index.js` | getProjectDb 中自动检测并执行碎片迁移（仅首次，表为空时触发） |

### Phase 2: IPC 与 Preload 层 ✅

| 模块 | 文件 | 内容 |
|------|------|------|
| IPC 模块 | `src/main/ipc/knowledge.js` | 11 个通道：list/get/create/update/delete/addLink/removeLink/getLinks/getLinkedItems/search/stats |
| 主进程注册 | `src/main.js` | registerKnowledgeIpc 注入 + emitKnowledgeChanged 广播 + clipboardImageCache 传递 |
| Preload 暴露 | `src/preload.js` | window.ipm.knowledge 命名空间（12 个方法 + subscribe 订阅） |
| 事件广播 | `src/main.js` | emitClipboardRecordChanged 并行广播 knowledge:changed（兼容期） |

### Phase 3: 核心 UI ✅

| 模块 | 文件 | 内容 |
|------|------|------|
| BlockNote 依赖 | `package.json` | @blocknote/core, @blocknote/react, @blocknote/mantine, @mantine/core, @mantine/hooks |
| 富文本编辑器 | `components/knowledge/NoteEditor.jsx` | BlockNote 封装组件，支持 initialContent、onChange(json+text)、readOnly |
| 知识管理主页 | `components/knowledge/KnowledgePage.jsx` | 双模式统一页面（管理模式 + 关联模式），无独立侧边栏 |
| 工具栏 | `components/knowledge/KnowledgeToolbar.jsx` | 视图切换（管理/关联）+ 类型筛选 + 搜索 + 新建下拉 |
| 碎片卡片 | `components/knowledge/KnowledgeItemCard.jsx` | forwardRef 卡片，支持管理模式点击 + 关联模式拖拽/选择 |
| 详情面板 | `components/knowledge/KnowledgeDetailPanel.jsx` | 右侧滑出面板（两种模式共享） |
| 路由集成 | `ProjectManager.jsx` + `RootTable.jsx` | knowledgeCtx 状态 + "知识管理"按钮（indigo 配色，BookOpen 图标） |

### Phase 4: 存量改造 ✅

| 模块 | 文件 | 内容 |
|------|------|------|
| SnippetLinker 改造 | `SnippetLinkerMockPage.jsx` | 数据源→knowledge/list，关联→addLink/removeLink，删除→knowledge/delete，订阅→knowledge.subscribe |
| SnippetCard 改造 | `SnippetCard.jsx` | 新增类型标签（截图/笔记），截图类型渲染缩略图 |
| TrayWidget 改造 | `TrayWidget.jsx` | 文本保存→knowledge/create(type=snippet)，截图保存→knowledge/create(type=screenshot, token=...) |
| 旧 IPC 兼容 | `snippets.js` + `screenshots.js` | 所有旧 handler 添加 console.warn('[DEPRECATED]')，暂时保留不删除 |

### Phase 5: 页面合并 — 整理知识 → 知识管理 ✅

| 模块 | 文件 | 内容 |
|------|------|------|
| 关联逻辑 hook | `components/knowledge/useKnowledgeLinker.js` | 从 SnippetLinkerMockPage 提取文件树加载、拖拽关联、选择、撤销、toast 等逻辑 |
| KnowledgePage 双模式 | `components/knowledge/KnowledgePage.jsx` | 增加「关联」视图模式，内嵌文件树面板 + ConnectorLines + 可拖拽卡片 |
| KnowledgeToolbar | `components/knowledge/KnowledgeToolbar.jsx` | 新增「管理/关联」视图切换按钮组 |
| KnowledgeItemCard | `components/knowledge/KnowledgeItemCard.jsx` | 升级为 forwardRef，支持 draggable/isSelected/linkedFileName/onUnlink 等关联模式 props |
| RootTable 精简 | `RootTable.jsx` | 移除「整理知识」按钮，仅保留「知识管理」单一入口 |
| ProjectManager 清理 | `ProjectManager.jsx` | 移除 snippetLinkerCtx 状态 + SnippetLinkerMockPage 导入 + 旧路由条件 |

### Phase 6: Webclip 网页剪藏 ✅

| 模块 | 文件 | 内容 |
|------|------|------|
| Readability 服务 | `Agent/services/webclip.js` | 新建：fetchAndExtract(url) 使用 fetch+JSDOM+Readability 提取网页正文，summarizeContent(text) 调用 LLM 生成中文摘要 |
| 依赖安装 | `package.json` | 新增 @mozilla/readability、jsdom |
| Vite 配置 | `vite.main.config.mjs` | external 增加 canvas、jsdom（jsdom 依赖大量静态资源，不可被 Vite 打包） |
| IPC: createWebclip | `src/main/ipc/knowledge.js` | 接收 URL → fetchAndExtract → summarizeContent → createItem(type=webclip) → 广播 |
| IPC: addWebclipImage | `src/main/ipc/knowledge.js` | 接收 pngBuffer → 写入 screenshots 目录 → 更新 content_json.images 数组 |
| IPC: get (增强) | `src/main/ipc/knowledge.js` | webclip 类型自动解析 content_json.images 为绝对路径 _resolvedImages |
| IPC: stats (增强) | `src/main/ipc/knowledge.js` | 增加 webclips 计数 |
| content_text 修复 | `src/main/ipc/knowledge.js` | webclip 与 note 一样存全文（不截断为 200 字预览） |
| Preload | `src/preload.js` | 暴露 createWebclip、addWebclipImage 方法 |
| Toolbar 入口 | `KnowledgeToolbar.jsx` | TYPE_TABS 增加「网页」分类，新建下拉增加「网页剪藏」(Globe 图标) |
| 创建弹窗 | `KnowledgePage.jsx` | URL 输入框 + 抓取按钮 + 加载/成功/错误状态反馈，stats 增加网页计数 |
| 卡片样式 | `KnowledgeItemCard.jsx` | webclip: Globe 图标、bg-blue-50 蓝色徽标、摘要优先显示、底部来源域名 |
| 详情视图 | `KnowledgeDetailPanel.jsx` | webclip 分支：来源 URL(可外部打开) + AI 摘要 + 正文预览 + 截图上传/展示 + OCR 预告 + 转笔记 |

**Webclip 数据模型**：复用 `content_json` 字段存储结构化元数据：
```json
{
  "images": ["snippets/screenshots/webclip_xxx_1.png"],
  "readability": { "siteName": "...", "excerpt": "..." },
  "_resolvedImages": ["D:/proj/.../webclip_xxx_1.png"]  // get 时动态填充
}
```

**三层架构设计**（已实现第一层 + 第三层）：
- ✅ 第一层：fetch + JSDOM + Readability 正文提取 + AI 摘要
- ⏳ 第二层：fetch 失败时利用 Electron BrowserWindow 渲染页面（待开发）
- ✅ 第三层：用户手动上传截图到 webclip（OCR 识别待后续统一开发）

---

## 五、截图缩略图方案

截图物理文件在 `snippets/screenshots/*.png`，前端渲染方案：
- `knowledge/list` 和 `knowledge/get` 返回截图时，通过 `resolveInside` 将相对路径转为绝对路径，附在 `_absolutePath` 字段
- **已使用 `ipm-file://` 自定义协议**（`src/main.js` 中注册为 privileged scheme，`protocol.handle` 拦截请求，`net.fetch` 提供文件流）
- 前端统一使用 `<img src="ipm-file:///D:/..." />` 渲染，绕过 Vite 开发模式下的 CSP 和 Windows 盘符路径问题
- Webclip 截图同理：`content_json._resolvedImages` 中的绝对路径 + `ipm-file:///` 前缀

---

## 六、未完成 / 待开发

### 6.1 OCR（文字识别）— 未接入

**现状**：截图碎片仅存图 + 手动标注，无自动文字提取能力。

**调研方向**：
- PaddleOCR：已走通 MVP，使用 PaddlePaddle 模型下载到本地，通过 Python SDK 做纯本地 CPU OCR
- 集成方案待定：可通过 Electron 调用本地 Python 子进程，或编译为独立二进制
- 集成时机：等知识碎片核心功能稳定后再接入

### 6.2 知识库大屏 KnowledgePanorama — Mock 数据

**现状**：侧边栏"知识库"入口仍渲染 `KnowledgePanorama`（全部 Mock 数据），未接入真实数据。

**计划**：等知识碎片功能齐全后，将 KnowledgePanorama 改造为跨项目的知识总览仪表盘，数据源接入各项目的 knowledge API。

### 6.3 AI 与知识碎片联动 — 未开始

**现状**：AI Agent 与知识碎片完全脱钩。

**未来方向**：
- AI 自动打标签 / 生成摘要
- 智能关联推荐（根据内容相似度推荐关联的文件/碎片）
- 基于知识碎片的问答

**集成时机**：等知识碎片功能基本成型后再开发。

### 6.4 Webclip 类型 — ✅ 已实现（v3）

**已完成**：第一层（fetch + Readability 正文提取 + AI 摘要）+ 第三层（用户上传截图到 webclip）。
**待开发**：第二层（Electron BrowserWindow 渲染，应对反爬/登录页面）、OCR 自动识别截图内容。

### 6.5 旧 IPC 清理

**现状**：`snippets/*` 和 `screenshots/*` IPC 通道已添加 `[DEPRECATED]` 警告，暂时保留。

**计划**：确认所有 UI 代码路径均已切换到 `knowledge/*` 后，在后续版本移除旧通道及对应的 JSON 文件读写逻辑。

---

## 七、文件清单

| 状态 | 文件路径 | 说明 |
|------|---------|------|
| 修改 | `desktop/Agent/db/init.js` | Schema v3 迁移 |
| 新建 | `desktop/Agent/db/knowledge.js` | 数据访问层 |
| 修改 | `desktop/Agent/db/migrate.js` | JSON→SQLite 迁移 |
| 修改 | `desktop/Agent/db/index.js` | 碎片迁移调用 |
| 新建 | `desktop/src/main/ipc/knowledge.js` | IPC 模块 |
| 修改 | `desktop/src/main.js` | 注册 + 广播 |
| 修改 | `desktop/src/preload.js` | knowledge API 暴露 |
| 新建 | `desktop/src/ui/components/knowledge/KnowledgePage.jsx` | 主页面 |
| 新建 | `desktop/src/ui/components/knowledge/KnowledgeToolbar.jsx` | 工具栏 |
| 新建 | `desktop/src/ui/components/knowledge/KnowledgeItemCard.jsx` | 卡片组件（双模式 forwardRef） |
| 新建 | `desktop/src/ui/components/knowledge/KnowledgeDetailPanel.jsx` | 详情面板 |
| 新建 | `desktop/src/ui/components/knowledge/NoteEditor.jsx` | BlockNote 编辑器 |
| 新建 | `desktop/src/ui/components/knowledge/useKnowledgeLinker.js` | 关联逻辑 hook |
| 修改 | `desktop/src/ui/components/ProjectManager.jsx` | KnowledgePage 入口（已移除 SnippetLinker 路由） |
| 修改 | `desktop/src/ui/components/project-manager/RootTable.jsx` | 知识管理按钮（已移除整理知识按钮） |
| 保留 | `desktop/src/ui/components/snippetlinker/SnippetLinkerMockPage.jsx` | 旧独立页面（代码保留但已无入口引用） |
| 修改 | `desktop/src/ui/components/snippetlinker/SnippetCard.jsx` | 截图支持 + ipm-file 协议 |
| 修改 | `desktop/src/ui/components/floating/TrayWidget.jsx` | 保存链路切换 |
| 修改 | `desktop/src/main/ipc/snippets.js` | DEPRECATED 警告 |
| 修改 | `desktop/src/main/ipc/screenshots.js` | DEPRECATED 警告 |
| 新建 | `desktop/Agent/services/webclip.js` | Readability 正文提取 + AI 摘要服务 |
| 修改 | `desktop/vite.main.config.mjs` | external 增加 canvas、jsdom |
| 修改 | `desktop/package.json` | BlockNote + Mantine + @mozilla/readability + jsdom 依赖 |
| 重写 | `desktop/src/ui/components/knowledge/KnowledgePanorama.jsx` | v4 跨项目全景页（总览+管理双视图） |
| 修改 | `desktop/src/ui/components/knowledge/KnowledgeItemCard.jsx` | v4 项目来源标签 + 草稿样式 |
| 修改 | `desktop/src/ui/components/knowledge/KnowledgeDetailPanel.jsx` | v4 草稿归属按钮 |
| 修改 | `desktop/src/ui/App.jsx` | v4 传递导航回调给全景页 |
| 修改 | `desktop/index.html` | v4 新增 fadeIn 全局动画 |
| 修改 | `desktop/Agent/db/board.js` | v7 deleteGroup 坐标回转 + copyBoardToGroup + copyGroupToBoard |
| 修改 | `desktop/src/main/ipc/knowledge.js` | v7 新增 convertBoardToGroup / convertGroupToBoard IPC |
| 修改 | `desktop/src/preload.js` | v7 新增 convertBoardToGroup / convertGroupToBoard API |
| 重构 | `desktop/src/ui/components/knowledge/BoardCanvas.jsx` | v7 forwardRef + localGroups + absItems + 相对坐标系 + undo/redo 快捷键 |
| 重构 | `desktop/src/ui/components/knowledge/GroupFrame.jsx` | v7 白板容器风格 + scale 适配 + onDragEnd + 转看板按钮 |
| 修改 | `desktop/src/ui/components/knowledge/WabiBoardPage.jsx` | v7 ConfirmDialog + GroupNavigator + undo/redo 系统 + 互转 handler |
| 新建 | `desktop/src/ui/components/project-manager/CreateKnowledgeModal.jsx` | v8 右键新建知识碎片弹窗（文本/截图/网页 + 自动关联 + 跳转提示） |
| 修改 | `desktop/src/ui/components/project-manager/hooks/useContextMenu.js` | v8 添加"新建知识碎片"菜单项 + onCreateKnowledge 回调 |
| 修改 | `desktop/src/ui/components/ProjectManager.jsx` | v8 createKnowledgeTarget 状态 + CreateKnowledgeModal 集成 |
| 修改 | `desktop/src/ui/components/knowledge/KnowledgeDetailPanel.jsx` | v8 加入临时看板按钮（LayoutDashboard） |
| 修改 | `desktop/src/ui/components/knowledge/NoteEditorPage.jsx` | v8 加入临时看板按钮 |
| 修改 | `desktop/src/ui/components/knowledge/KnowledgePage.jsx` | v8 handleAddToTempBoard 逻辑 + toast |
| 新建 | `desktop/src/ui/components/knowledge/CardReaderModal.jsx` | v8 阅览弹窗（毛玻璃 + Markdown 渲染 + 多类型适配 + 导航按钮） |
| 新建 | `desktop/src/ui/components/knowledge/ViewerToolbar.jsx` | v8 阅览模式筛选搜索浮动工具栏 |
| 修改 | `desktop/src/ui/components/knowledge/BoardCanvas.jsx` | v8 viewerMode + 高亮/筛选状态 + 平滑导航 + 手动双击检测 + height wrapper 修复 |
| 修改 | `desktop/src/ui/components/knowledge/ConnectionLayer.jsx` | v8 高亮/筛选视觉效果 |
| 修改 | `desktop/src/ui/components/knowledge/GroupFrame.jsx` | v8 highlightState + onViewerClick |
| 修改 | `desktop/src/ui/components/knowledge/WabiBoardPage.jsx` | v8 viewerMode 切换 + CardReaderModal/ViewerToolbar 集成 |
| 重写 | `desktop/src/ui/components/knowledge/KnowledgePanorama.jsx` | v8 SVG minimap 看板预览 + InsightCarousel + UnderConstructionCard |

---

## 八、关键设计决策备忘

1. **存储位置**：碎片表直接加入现有的每项目 `project.db`，不另建独立 DB
2. **物理文件不变**：文本仍写 `snippets/clipboard/*.txt`，截图仍写 `snippets/screenshots/*.png`，仅索引从 JSON 迁移到 SQLite
3. **笔记类型不写物理文件**：type=note 的内容全部存在 content_json（BlockNote JSON）+ content_text（纯文本导出）
4. **关联表多对多**：一条碎片可关联多个文件/文件夹，替代原来 linkedTo 单一字段
5. **编辑器选型**：BlockNote（Mantine 版），存储为 BlockNote JSON（无损），导出纯文本用于搜索
6. **侧边栏"知识库"已接入真实数据**：KnowledgePanorama 从 mock 重构为跨项目聚合中台（v4），无内部侧边栏
7. **截图 token 机制**：knowledge/create 支持通过 clipboardImageCache token 获取 PNG Buffer，兼容悬浮窗的截图保存流程
8. **页面合并策略**：将「整理知识」(SnippetLinkerMockPage) 的关联功能整合进「知识管理」(KnowledgePage)，通过 toolbar 视图切换实现双模式（管理/关联），复用 FileTree、AssociationNodeList、ConnectorLines 等组件，关联逻辑提取为 useKnowledgeLinker hook
9. **SnippetLinkerMockPage 保留不删**：代码文件保留（但 ProjectManager 不再导入），方便回滚或参考
10. **ipm-file:// 自定义协议**：在 main.js 中注册为 privileged scheme（bypassCSP + stream + supportFetchAPI），通过 `protocol.handle` + `net.fetch` 提供本地文件流，解决 Vite 开发模式下 file:// 被 CSP 阻止 + Windows 盘符路径解析错误的问题
11. **Webclip 正文提取选型**：采用 Mozilla Readability + JSDOM（Firefox 阅读模式同款算法），Node.js 端纯服务端提取，30s 超时，带 User-Agent；Readability 失败时降级为 stripHtmlTags 的原始文本
12. **Webclip AI 摘要策略**：调用已有 createSummaryModel() LLM 服务，输入截断 6000 字符，try/catch 包裹，失败返回空字符串（不阻塞主流程）
13. **Webclip 截图存储**：复用 content_json 字段存储 images 数组（相对路径），get 时动态解析为 _resolvedImages（绝对路径），不新建表或字段
14. **Webclip 三层降级设计**：① fetch+Readability → ② Electron BrowserWindow 渲染（待开发）→ ③ 用户手动上传截图 + OCR（OCR 待开发）
15. **jsdom 不可被 Vite 打包**：jsdom 内部用 `__dirname` 定位静态资源（default-stylesheet.css 等），Vite 打包后路径错乱；canvas 为 jsdom 可选原生依赖，未安装。两者均加入 vite.main.config.mjs 的 external 列表
16. **跨项目聚合策略**：`listGlobal` 枚举 `projects/` 和 `cases/` 下所有子目录 + Study DB，逐个打开 DB 查询后合并排序，附加 `_projectName`、`_domain`、`_projectDir` 元数据
17. **草稿箱存储位置**：草稿存储在 Study 空间的 `project.db` 中，通过 `source_kind = 'draft'` 标识，与 Study 空间未来自有知识互不干扰
18. **草稿归属流程**：`assignDraft` 从 Study DB 读取完整 item → 在目标项目 DB 创建新 item（新 ID，`source_kind` 改为正常值）→ 复制物理文件 → 删除 Study DB 中原 item
19. **全景页双视图设计**：总览视图（中台仪表盘）+ 知识管理视图（筛选 + 搜索 + 标签聚合 + 草稿创建）。无内部侧边栏，无知识健康度，无时间线
20. **卡片项目标签**：KnowledgeItemCard 新增 `projectLabel`/`projectDomain` prop，底部显示来源项目胶囊。草稿卡片使用虚线边框 + 琥珀色角标

---

## 九、v4 — 知识库全景页重构

### 9.1 新增 IPC

| IPC 通道 | 说明 |
|---------|------|
| `knowledge/listGlobal` | 跨项目聚合查询，枚举所有 projects/cases/study，支持 type/search/projectName/domain/draftsOnly 筛选 |
| `knowledge/statsGlobal` | 全局统计，返回各类型计数 + byProject 分布 + tagDistribution |
| `knowledge/createDraft` | 在 Study DB 创建草稿（source_kind='draft'），支持 snippet/note/webclip/screenshot |
| `knowledge/assignDraft` | 将草稿从 Study DB 转移到目标项目 DB，复制物理文件，删除原草稿 |
| `knowledge/listDrafts` | 仅查询 Study DB 中的草稿 |
| `knowledge/listProjects` | 列出所有可用的项目和案件（供草稿归属 UI 使用） |

### 9.2 前端变动

| 文件 | 变动 |
|------|------|
| `KnowledgePanorama.jsx` | 完全重写：删除 661 行 mock → 真实数据 + 两视图（总览/知识管理）。总览视图包含统计卡片（数字滚动动画）、标签分布柱状图、最近新增列表、项目知识分布图。知识管理视图包含类型筛选 Tab、项目/草稿筛选下拉、搜索框（focus 展开动效）、标签聚合开关、新建菜单（4 种类型）、卡片网格（staggered fadeIn）、草稿归属弹窗 |
| `KnowledgeItemCard.jsx` | 新增 `projectLabel`/`projectDomain`/`onNavigateToProject` 三个 prop；草稿卡片虚线边框 + 琥珀色角标；来源项目胶囊标签；hover 效果增强（-translate-y-0.5 + shadow 升级） |
| `KnowledgeDetailPanel.jsx` | 新增 `isDraft`/`onAssignDraft` prop；草稿时显示"归属到项目"醒目按钮（虚线 indigo 边框）；Header 增加草稿标签；来源区域显示"草稿" |
| `App.jsx` | 向 KnowledgePanorama 传递 `onNavigateToProject` 回调，点击卡片来源标签可跳转到对应分区 |
| `preload.js` | 新增 6 个 API：listGlobal、statsGlobal、createDraft、assignDraft、listDrafts、listProjects |
| `index.html` | 新增 `@keyframes fadeIn` + `.animate-fadeIn` 全局动画 |

### 9.3 UI/UX 设计要点

- 配色遵循 OverviewPage 风格：白底、slate 色系、indigo 强调色
- 统计数字使用 requestAnimationFrame 驱动的 AnimatedNumber 组件（ease-out cubic，400ms）
- 视图切换使用胶囊式 Tab（bg-slate-100 容器 + active bg-white shadow-sm）
- 卡片 staggered fadeIn（每张延迟 30ms）+ hover 上移 + shadow 增强
- 搜索框 focus 时 w-48→w-64 平滑过渡
- 标签分布图使用水平条形 + indigo/slate 渐变 + 延迟入场动画
- 草稿归属弹窗：居中模态、项目/案件分组列表、搜索过滤、选中态 indigo 高亮

---

## 十、v5 — Wabi-Sabi Board 侘寂看板

### 10.1 概述

在知识库全景页的总览视图中嵌入 Wabi-Sabi Board（自由画布看板），支持多看板管理、拖拽/旋转/缩放知识碎片卡片、画布布局持久化。同时重构总览视图布局，使其更加充实。

### 10.2 存储方案

在 Study 空间的 `project.db` 新增两张表（v4 迁移）：

| 表 | 用途 |
|----|------|
| `boards` | 看板元数据（id, name, is_main, created_at, updated_at） |
| `board_items` | 看板上的卡片布局（id, board_id, knowledge_id, source_project, source_domain, x, y, rotation, width, height, z_index, created_at） |

### 10.3 新增 IPC

| IPC 通道 | 说明 |
|---------|------|
| `board/list` | 列出所有看板，按 is_main DESC, created_at DESC |
| `board/create` | 创建看板，第一个自动设为 is_main |
| `board/rename` | 重命名看板 |
| `board/delete` | 删除看板（不允许删主看板），CASCADE 删 board_items |
| `board/setMain` | 设为主看板（旧主看板取消） |
| `board/getItems` | 查询看板所有 board_items，跨项目 JOIN knowledge_items 获取碎片内容 |
| `board/addItem` | 添加知识碎片到看板，自动 z_index 递增 |
| `board/removeItem` | 从看板移除卡片 |
| `board/updateLayout` | 批量更新卡片布局（拖拽/旋转/缩放结束时调用） |
| `board/createAndAdd` | 在看板上直接新建碎片（createDraft + addItem） |
| `board/stats` | 看板统计（每个看板的 itemCount） |

### 10.4 前端文件变动

| 文件 | 变动 |
|------|------|
| `BoardCard.jsx` | **新建**。侘寂风格卡片渲染组件，按碎片类型映射：snippet→暖黄纸片、webclip→奶油色带URL、note→白色日志带渐变遮罩、screenshot→拍立得、draft→淡黄图钉 |
| `BoardCanvas.jsx` | **新建**。画布核心组件，React pointer events 实现拖拽/旋转/缩放。选中态显示旋转手柄（底部圆形）+ 四角 resize handle + 删除按钮。pointerup 时批量 flush 布局 |
| `WabiBoardPage.jsx` | **新建**。全屏看板页面：64px Header（返回+看板名+时间筛选+收集+新建）、flex-1 Canvas、40px Footer（保存状态+看板切换+类型统计）。含 CollectModal（搜索+筛选全局碎片添加）、QuickCreateModal（新建文本/笔记/网页碎片）、CreateMenu、BoardSwitcher |
| `KnowledgePanorama.jsx` | 新增 `boardOpen` 状态，import WabiBoardPage + BoardCanvas。总览视图重构为三行：Row1 统计卡片、Row2 看板预览(2/3)+最近新增+标签分布(1/3)、Row3 项目分布。新增 BoardPreviewCard 组件（缩略画布预览+hover遮罩+打开按钮） |
| `preload.js` | 新增 `window.ipm.board` 命名空间（11 个 API：list/create/rename/delete/setMain/getItems/addItem/removeItem/updateLayout/createAndAdd/stats） |
| `init.js` | CURRENT_VERSION → 4，新增 MIGRATION_V4（boards + board_items 表） |
| `board.js` | **新建**。看板数据访问层 DAL，提供 listBoards/getBoard/createBoard/renameBoard/deleteBoard/setMainBoard/getBoardItems/addBoardItem/removeBoardItem/updateBoardLayout/countBoardItems |
| `knowledge.js` | 新增 board/* IPC handlers，import board DAL，复用 getStudyDb + enumerateWorkspaceDirs + makeShortId |
| `index.html` | 新增 `.wabi-canvas`、`.wabi-grid-bg`、`@keyframes wabi-card-in`、`@keyframes wabi-pulse` CSS |

### 10.5 配色方案

遵循设计指南的侘寂美学 token，融入现有 slate/indigo 框架：

- 画布背景：`#F9F8F4`（米色）
- 方格点阵：`rgba(0,0,0,0.03)` 40px
- 卡片：暖黄 `#FFFBE0`（snippet）、奶油 `#FFFDF2`（webclip）、白色（note）、拍立得白色（screenshot）、淡黄 `#FFF9E6`（draft）
- 强调色：`#4a9e8e`（青绿）用于选中态、操作手柄、按钮
- Header/Footer 保持应用的 slate 色系

### 10.6 交互系统

- **拖拽移动**：pointerdown → pointermove → pointerup，拖动时 localItems 实时更新位置
- **旋转**：选中后底部圆形手柄，atan2 计算角度差
- **缩放**：四角 resize handle（se/sw/ne/nw），最小 120px
- **选中态**：1.5px accent outline + 显示旋转/resize/删除手柄 + 底部操作提示
- **布局持久化**：pointerup 时通过 `board/updateLayout` 批量保存
- **多看板**：Footer BoardSwitcher 下拉切换，Header 更多菜单管理（重命名/设为主看板/新建/删除）

### 10.7 关键设计决策

21. **看板存储选型**：SQLite 新表优于 JSON 文件，因为看板与碎片是多对多关系，布局数据需按 board_id 高效索引查询
22. **存储位置**：Study 空间的 `project.db`，与草稿箱一致，全局作用域
23. **总览视图重构**：原两列（标签+最近）改为三行布局：统计→看板预览(2/3)+右侧堆叠(1/3)→项目分布，信息密度更高
24. **看板预览卡片**：使用 BoardCanvas readOnly 模式 + CSS scale(0.45) 缩略渲染，hover 时显示半透明遮罩+打开按钮
25. **交互系统纯 React**：不依赖 D3/Konva 等库，使用原生 pointer events + state 管理，减少依赖体积
26. **碎片跨项目引用**：board_items 通过 source_project + source_domain 定位碎片所在 DB，getItems 时逐 DB 读取并合并

---

## 十一、v6 — Wabi-Sabi Board 二期（无限画布 / 多选 / 连线 / 分组 / 锁定 / 视觉层）

### 11.1 存储变更（Schema v5 迁移）

`CURRENT_VERSION` 升至 5，新增 `MIGRATION_V5`：

| 变更 | 内容 |
|------|------|
| 新表 `board_connections` | `id, board_id, from_item_id, to_item_id, color, created_at` — 卡片间连线 |
| 新表 `board_groups` | `id, board_id, name, x, y, width, height, color, z_index, created_at` — 可视分组区域框 |
| `board_items` 新列 | `locked INTEGER DEFAULT 0`（锁定状态）、`group_id TEXT DEFAULT NULL`（所属分组） |
| `boards` 新列 | `bg_style TEXT DEFAULT 'grid'`（背景样式）、`bg_color TEXT DEFAULT ''`（自定义背景色） |

### 11.2 数据层扩展（board.js）

新增 DAL 函数：

- 连线：`listConnections` / `addConnection` / `removeConnection`
- 分组：`listGroups` / `createGroup` / `updateGroup` / `deleteGroup`
- 锁定：`lockItem` / `unlockItem`
- 样式：`updateBoardStyle`
- `updateBoardLayout` 扩展支持 `groupId` 字段

### 11.3 新增 IPC

| IPC | 用途 |
|-----|------|
| `board/listConnections` | 查询看板所有连线 |
| `board/addConnection` | 创建连线 |
| `board/removeConnection` | 删除连线 |
| `board/listGroups` | 查询看板所有分组 |
| `board/createGroup` | 创建分组区域框 |
| `board/updateGroup` | 更新分组位置/大小/名称/颜色 |
| `board/deleteGroup` | 删除分组（自动解除卡片归属） |
| `board/lockItem` | 锁定卡片 |
| `board/unlockItem` | 解锁卡片 |
| `board/updateBoardStyle` | 更新画布背景样式 |

### 11.4 前端新组件

| 文件 | 说明 |
|------|------|
| `ConnectionLayer.jsx` | SVG 覆盖层，淡红色贝塞尔曲线（二次贝塞尔，控制点下垂模拟绳子悬垂）。端点追踪卡片中心。点击宽 hit-area 可删除连线 |
| `GroupFrame.jsx` | 半透明矩形区域框，可拖拽移动、四角缩放。双击名称编辑。卡片拖入后自动归组 |
| `Minimap.jsx` | 120x80px 半透明面板，固定右下角。缩小渲染卡片为彩色小块（按类型着色），显示当前视窗矩形，点击跳转 |

### 11.5 BoardCanvas 重构

| 特性 | 实现 |
|------|------|
| **无限画布** | `viewportX/Y/scale` 状态 + CSS `transform` 外层容器。仅主看板 (`isMain`) 启用 |
| **缩放** | `onWheel` 以鼠标位置为中心，范围 0.2~3.0 |
| **平移** | 空格键+鼠标拖拽 / 中键拖拽 |
| **多选** | Shift+点击 toggle、空白区域拖拽框选虚线矩形 |
| **键盘快捷键** | Delete 删除、Ctrl+A 全选、Escape 取消、Ctrl+L 锁定/解锁 |
| **多选工具栏** | 选中 >1 项时顶部居中浮现：连线(2选)、分组、锁定、删除 |
| **编辑导航** | 选中卡片左上角 Pencil 图标 → `onEditCard` → `onBack({ navigate })` → KnowledgePanorama 自动切换管理视图并填充搜索 |
| **卡片锁定** | locked 卡片右上角锁图标、禁止拖拽/旋转/缩放、选中态仅显示解锁按钮 |
| **自动归组** | 拖拽结束时检测卡片中心是否在分组框内，自动设置 `group_id` |
| **背景自定义** | 支持 grid（方格线）、dots（点阵）、blank（纯净）、color（自定义色） |

### 11.6 BoardCard 扩展

- **标签胶囊**：底部显示彩色胶囊，基于标签名 hash 分配柔和色系（10色调色板），最多 3 个 + `+N`

### 11.7 KnowledgePanorama 扩展

- **编辑导航处理**：WabiBoardPage `onBack` 可携带 `{ navigate: { project, domain, search } }`，关闭看板后自动切换到管理视图并设置筛选条件
- **学习分区入口**：`studyKnowledgeOpen` 状态，草稿箱筛选态下显示 "管理学习碎片" 按钮 → 打开 `StudyKnowledgeList` 全屏列表

### 11.8 关键设计决策

27. **无限画布仅限主看板**：次级看板保持固定画布，降低认知负荷
28. **连线用贝塞尔曲线模拟绳子**：二次贝塞尔 Q 指令，控制点 y 坐标下沉 `min(dist*0.25, 80)` 实现自然悬垂
29. **分组自动归属**：拖拽结束时检测卡片中心是否在任意 GroupFrame 内，比按钮操作更自然
30. **编辑导航复用现有 ManageView**：不额外设计编辑页面，而是跳转回知识管理视图并自动填充搜索词，一举两得
31. **标签染色采用 hash 分配**：固定 10 色调色板，按字符 hash 取模确定颜色，确保同一标签始终同色

---

## 十二、v7 — Wabi-Sabi Board 三期（分组重设计 / 相对坐标系 / 看板互转 / 组导航 / 撤回重做）

### 12.1 概述

将分组从"视觉矩形框"升级为"交互式白板容器"——移动分组自动带动所有组内卡片、卡片拖入拖出时自动坐标转换、禁止嵌套分组。同时实现看板与组的副本互转、组导航快速定位、删除确认弹窗、以及内存级撤回/重做系统。

### 12.2 核心架构变更：相对坐标系

**之前**：所有卡片使用画布绝对坐标，`group_id` 仅用于视觉归属标记，移动分组不影响卡片位置。

**之后**：
- **组内卡片使用相对坐标**：`board_items.x/y` 对于有 `group_id` 的卡片，表示相对于组左上角的偏移
- **渲染时计算绝对位置**：`absoluteX = group.x + item.x`, `absoluteY = group.y + item.y`
- **移动组时**：仅更新 `group.x/y`，所有组内卡片自动跟随
- **拖入组时**：`card.x -= group.x; card.y -= group.y; card.group_id = group.id`
- **拖出组时**：`card.x += group.x; card.y += group.y; card.group_id = null`
- **跨组拖动**：先转出旧组（变绝对），再转入新组（变相对）
- **禁止嵌套**：创建分组时仅允许选中 `group_id = null` 的自由卡片

### 12.3 数据层变更（board.js）

| 变更 | 内容 |
|------|------|
| `deleteGroup` 修改 | 删除组前将组内卡片的相对坐标转为绝对坐标：`UPDATE SET x = x + group.x, y = y + group.y, group_id = NULL` |
| `copyBoardToGroup` 新增 | 将看板内容副本复制为主看板上的组：计算包围盒、创建 board_group、复制 items（坐标转为相对）、复制 connections（ID 映射） |
| `copyGroupToBoard` 新增 | 将组内容副本复制为新独立看板：创建 board、复制 items（相对坐标直接作为新看板位置）、复制内部 connections |

### 12.4 新增 IPC

| IPC | 用途 |
|-----|------|
| `board/convertBoardToGroup` | 将非主看板内容副本转为主看板上的分组，自动获取主看板 ID |
| `board/convertGroupToBoard` | 将分组内容副本转为新独立看板，以组名命名 |

### 12.5 Preload 新增

`window.ipm.board` 新增：
- `convertBoardToGroup(sourceBoardId, groupX, groupY)` — 看板转组（副本）
- `convertGroupToBoard(groupId)` — 组转看板（副本）

### 12.6 前端变动

| 文件 | 变动 |
|------|------|
| `BoardCanvas.jsx` | **重大重构**。改为 `forwardRef` 组件，暴露 `focusOnRegion` 方法。新增 `localGroups` 状态实现组拖拽的本地实时更新。新增 `absItems` memo 计算绝对坐标供 ConnectionLayer/Minimap 使用。`handlePointerUp` 自动分组逻辑加入坐标转换。`handleGroupSelected` 改为 async，创建组后立即转换选中卡片为相对坐标。旋转中心、框选碰撞均使用绝对坐标。新增 Ctrl+Z/Ctrl+Shift+Z 快捷键绑定 undo/redo |
| `GroupFrame.jsx` | **全面重设计**。白板容器风格（半透明白色背景、模糊效果、标题栏区域）。拖拽/缩放除以 `scale` 因子适配无限画布。新增 `onDragEnd` 回调（拖拽结束时持久化）、`onConvertToBoard`（转为独立看板按钮）、`onRequestDeleteConfirm`（触发删除确认弹窗）。重命名不再触发 `onDragEnd` 避免闭包竞态 |
| `WabiBoardPage.jsx` | 新增 `ConfirmDialog` 通用确认弹窗组件（输入名称匹配确认），统一用于看板和组的删除。新增 `GroupNavigator` 下拉组件（主看板 header 显示所有组，点击自动缩放平移定位）。新增 undo/redo 双栈系统（undoStackRef/redoStackRef，50 步上限，纯内存）及 header 按钮。新增看板转组/组转看板的 handler 和菜单入口。`handleCreateGroup` 改为返回创建的 group 对象 |

### 12.7 GroupFrame 新设计

| 特性 | 实现 |
|------|------|
| 视觉风格 | 半透明白色背景 `rgba(255,255,255,0.55)` + `backdropFilter: blur(2px)` + hover 阴影升级 |
| 标题栏 | 顶部 28px 区域：GripHorizontal 图标 + 组名（双击编辑）+ 转为看板按钮 + 删除按钮 |
| Scale 适配 | 拖拽/缩放增量除以 `scale` 因子，确保在缩放画布上正确移动 |
| 持久化策略 | 拖拽中实时更新 `localGroups`（BoardCanvas 内部），拖拽结束时通过 `onDragEnd` 持久化到 DB |
| 重命名持久化 | 名称变更通过 `handleLocalGroupUpdate` 检测到 `name` 字段时立即持久化，不依赖 `onDragEnd` |

### 12.8 撤回/重做系统

| 项目 | 内容 |
|------|------|
| 架构 | 双栈模式（undoStack + redoStack），`useRef` 存储，50 步上限 |
| 存储 | 纯内存，切换看板或离开页面时清空 |
| Action 结构 | `{ type, undo: async () => {}, redo: async () => {} }` |
| 已追踪操作 | 删除卡片（undo 重新添加，redo 再次移除） |
| 快捷键 | `Ctrl+Z` 撤回、`Ctrl+Shift+Z` / `Ctrl+Y` 重做（BoardCanvas keydown 监听） |
| UI | Header 中 Undo2/Redo2 图标按钮，disabled 态自动跟踪栈深度 |

### 12.9 组导航

- **入口**：主看板 header 中 `GroupNavigator` 下拉按钮（Layers 图标 + "分组" 标签），仅在有分组时显示
- **交互**：点击组名 → `canvasRef.current.focusOnRegion({ x, y, width, height })`
- **算法**：根据组矩形和画布尺寸计算最佳 `scale`（留 60px padding），设置 `viewportX/Y` 使组居中

### 12.10 看板与组的互转

| 方向 | 触发入口 | 行为 |
|------|---------|------|
| 看板 → 组 | 非主看板菜单 "转为主看板分组" | 将看板所有 items/connections 副本复制到主看板，创建新组，坐标转相对。源看板保留 |
| 组 → 看板 | GroupFrame hover 时 ArrowRightLeft 按钮 | 将组内 items/connections 副本复制到新看板。原组保留 |
| 重复策略 | 允许同一知识卡片在多处出现 | 用户自行管理重复 |

### 12.11 删除确认机制

通用 `ConfirmDialog` 组件同时服务于看板删除和组删除：
- 模态遮罩 + 居中弹窗
- 显示目标名称（红色高亮）
- 要求用户在 input 中精确输入名称
- 匹配后才允许点击确认按钮
- 支持 Enter 快速确认、Escape 关闭

### 12.12 关键设计决策

32. **相对坐标系**：组内卡片存储相对于组左上角的偏移坐标，渲染时加上组坐标得到绝对位置。移动组只需更新组坐标，所有组内卡片自动跟随，性能优且逻辑清晰
33. **localGroups 本地状态**：BoardCanvas 维护 `localGroups` 本地状态，GroupFrame 拖拽时实时更新（不经过 DB round-trip），拖拽结束时才持久化，保证拖拽流畅
34. **禁止嵌套分组**：创建分组时过滤掉已有 `group_id` 的卡片，仅自由卡片可参与新组。符合"墙上贴白板、白板上不能再贴白板"的比喻
35. **deleteGroup 原子坐标转换**：在 DAL 层通过 SQL `SET x = x + group.x, y = y + group.y` 原子操作完成坐标回转，避免前后端不一致
36. **看板/组互转为副本模式**：两个方向都是复制而非移动，源看板/组保留。允许知识卡片重复出现，给用户最大灵活性
37. **undo/redo 纯内存架构**：使用 `useRef` 存储 action 栈，不持久化到 DB。切换看板/离开页面时自动清空。50 步上限控制内存占用
38. **focusOnRegion 通过 ref 暴露**：BoardCanvas 使用 `forwardRef` + `useImperativeHandle` 暴露 `focusOnRegion` 方法，父组件通过 ref 调用，避免 prop drilling 视窗状态
39. **ConfirmDialog 组件通用化**：统一的确认弹窗模式（输入名称匹配），同时用于看板删除和组删除，减少重复代码
40. **重命名与拖拽持久化分离**：GroupFrame 的 `commitName` 不调用 `onDragEnd`，而是通过 `handleLocalGroupUpdate` 检测到 `name` patch 时直接持久化，避免 React state 批处理导致的闭包读取旧值问题

---

## 十三、v8 — 右键新建知识碎片 / 临时看板 / 阅览模式 / 总览重设计

### 13.1 概述

本版本包含三大功能模块和一次总览页面重设计：

| 模块 | 核心内容 |
|------|---------|
| Feature 1 | 项目/案件页面右键菜单新建知识碎片（文本/截图/网页），自动关联右键目标，创建后可跳转知识管理页 |
| Feature 2 | 知识详情面板/笔记编辑器增加"加入临时看板"按钮，自动查找/创建固定名称看板 |
| Feature 3 | 主看板阅览模式（只读锁定 + 双击阅览弹窗 + 单击高亮连线网络 + 平滑导航 + 筛选搜索工具栏） |
| 总览重设计 | 看板预览改为 SVG minimap，信息面板改为 Insight Carousel + 施工中卡片 |

### 13.2 Feature 1：右键新建知识碎片

#### 涉及文件

| 文件 | 变更 |
|------|------|
| `useContextMenu.js` | 接收 `onCreateKnowledge` 回调，在文件/文件夹/空白右键菜单中添加"新建知识碎片"菜单项 |
| `ProjectManager.jsx` | 新增 `createKnowledgeTarget` 状态管理，将 entry/projectName/domain 传递给模态框，渲染 `CreateKnowledgeModal` |
| `CreateKnowledgeModal.jsx` | **新建组件**，全屏毛玻璃遮罩 + 居中弹窗 |

#### CreateKnowledgeModal 设计

- **类型选择**：文本、截图（上传图片）、网页剪藏可选；笔记类型灰显并以 tooltip 提示用户"创建后可去知识库整理"
- **动态表单**：根据类型切换标题/内容输入、图片上传区、URL 输入
- **自动关联**：显示右键目标（文件/文件夹），创建成功后调用 `knowledge.addLink` 自动建立关联
- **创建后跳转**：弹出确认对话框，询问是否跳转到该项目的知识管理页面

### 13.3 Feature 2：加入临时看板

#### 涉及文件

| 文件 | 变更 |
|------|------|
| `KnowledgeDetailPanel.jsx` | 顶栏增加 `LayoutDashboard` 图标按钮，调用 `onAddToTempBoard(formData)` |
| `NoteEditorPage.jsx` | 工具栏增加同样的按钮，调用 `onAddToTempBoard(item)` |
| `KnowledgePage.jsx` | 实现 `handleAddToTempBoard`：`board.list()` → 查找/创建"临时看板" → `board.addItem()` → toast "已加入「临时看板」" |
| `KnowledgePanorama.jsx` | 同样集成 `handleAddToTempBoard` 逻辑 |

#### 逻辑流程

```
用户点击看板图标
  → list 所有 boards → find(name === '临时看板')
  → 若不存在：board.create('临时看板')
  → board.addItem({ boardId, knowledgeId, sourceProject, sourceDomain, x: random, y: random })
  → toast 提示
```

### 13.4 Feature 3：主看板阅览模式

#### 3A — 模式切换与只读解耦

| 文件 | 变更 |
|------|------|
| `WabiBoardPage.jsx` | `viewerMode` 状态 + Eye/EyeOff 切换按钮（仅主看板可见），阅览模式下隐藏编辑控件 |
| `BoardCanvas.jsx` | `effectiveReadOnly = readOnly \|\| viewerMode`，`enablePanZoom = isMain && (!readOnly \|\| viewerMode)` — 阅览模式只读但保留平移缩放；暗角渐变遮罩 |

#### 3B — 阅览弹窗 (CardReaderModal)

**新建组件** `CardReaderModal.jsx`：

- `position: fixed` 全屏遮罩，`backdropFilter: blur(16px)` 毛玻璃
- `scale(0.92)→scale(1)` + `opacity` 入场动画，350ms cubic-bezier
- 根据 `knowledge.type` 渲染不同内容：
  - 文本/笔记/草稿：`marked` 库 Markdown 渲染 + 自定义 CSS（`.reader-md`）
  - 网页摘录：URL 外链 + AI 摘要 + 原文 + 附属截图
  - 截图：大图预览
- 底部导航：前往所属项目/案件、打开关联文件/文件夹（支持多关联）
- 双击触发改为手动计时器检测（`viewerClickRef`），避免 `onClick` 状态更新干扰原生 `dblclick` 事件

#### 3C — 高亮连线网络

| 文件 | 变更 |
|------|------|
| `BoardCanvas.jsx` | `highlightedIds` / `highlightAnchor` / `filterMatchIds` 三组状态；`handleViewerClick` 遍历 connections 计算直接连线网络 |
| `ConnectionLayer.jsx` | 高亮连线加粗变色（`#4a9e8e`），非相关连线降低 opacity |
| `GroupFrame.jsx` | `highlightState` prop（`'anchor'` / `'connected'` / `'dimmed'` / `'filter-dimmed'` / `'normal'`），控制边框/阴影/opacity/grayscale |

单击卡片/组 → 该实体及所有直接连线实体高亮 → 其余实体淡化。再次点击空白区域清除高亮。

#### 3D — 平滑导航

`BoardCanvas.focusOnRegion({ x, y, width, height }, smooth)` 增加 `smooth` 参数：

- `smooth = true` 时使用 `requestAnimationFrame` + `easeInOutCubic` 插值动画（500ms）
- 阅览模式下从组导航下拉框触发时传入 `smooth = true`

#### 3E — 筛选搜索工具栏 (ViewerToolbar)

**新建组件** `ViewerToolbar.jsx`：

- 顶部居中浮动工具栏，`translateY` 滑入动画
- 搜索框：按标题/内容匹配
- 筛选下拉：
  - 知识类型（全部/文本/笔记/截图/网页）
  - 标签（从 items 中提取唯一标签列表）
  - 项目/案件（从 items 中提取唯一 source_project）
- "清除全部"按钮
- 筛选结果通过 `canvasRef.current.setFilterMatchIds(matchedIds)` 控制非匹配卡片淡化（opacity + grayscale）

### 13.5 总览页面重设计

#### 看板预览卡片 (BoardPreviewCard)

原方案用 `BoardCanvas` 缩放 0.45x 做预览，存在坐标偏移导致空白的问题。

新方案改为 **SVG minimap**：

- 并行加载 items / groups / connections
- 计算全部元素包围盒 → `viewBox` + `preserveAspectRatio="xMidYMid meet"` 自动适配
- 分组：虚线边框矩形，颜色随 `frame_style` 变化
- 连线：淡红色贝塞尔曲线
- 卡片：白底圆角矩形 + 顶部类型色条 + 截断标题文字 + 阴影滤镜
- 底部统计栏：各类型碎片数量 + 色点

#### Insight Carousel（洞察轮播）

替代原有的统计卡片条 + 三列信息卡，4 个面板循环展示：

| 面板 | 内容 |
|------|------|
| 知识分布 | 左侧大字号总数 + 右侧横向柱状图（5 种类型，渐变色条，入场展开动画） |
| 最近新增 | 列表 + 从右滑入的错开入场动画 |
| 标签分布 | 横向柱状图，前两名靛蓝渐变 |
| 项目分布 | 横向柱状图，按 domain 区分颜色 |

交互：6s 自动轮播 / hover 暂停 / 左右箭头 / 圆点指示器（当前项展宽为胶囊形） / 计数器

#### 施工中卡片 (UnderConstructionCard)

Carousel 右侧约 42% 宽度，淡灰绿色调施工提示：

- 45° 条纹背景（`#F8FAF9` / `#F0F5F3`）
- 半透明青绿虚线边框 + 四角装饰性直角框线
- 图标在 Hammer / Wrench / HardHat / Construction 间轮换（2.5s），微微摇晃动画
- "更多功能开发中" + Sparkles 图标 + 预告文案

### 13.6 Bug 修复

| Bug | 原因 | 修复 |
|-----|------|------|
| 编辑模式卡片高度缩放失效 | viewer mode 添加的内层 opacity wrapper div 缺少 `height: '100%'` 和 `overflow: 'hidden'`，CSS `height: 100%` 链断裂 | 给 wrapper div 添加 `height: '100%'` + `overflow: 'hidden'` |
| 阅览模式双击不弹出阅览页 | `onClick` 触发 `setHighlightedIds` 导致 React 重渲染，干扰浏览器原生 `dblclick` 事件检测 | 改用手动计时器检测双击：`viewerClickRef` 记录时间戳，400ms 内再次点击同一卡片视为双击 |

### 13.7 关键设计决策

41. **SVG minimap 替代 BoardCanvas 预览**：用 SVG `viewBox` 自动适配所有元素，避免了真实 Canvas 的视口偏移和性能问题
42. **手动双击检测替代原生 dblclick**：在 React 中同元素 onClick + onDoubleClick 共存时，onClick 的 setState 导致重渲染可能丢失 dblclick 事件，手动计时器更可靠
43. **effectiveReadOnly 与 enablePanZoom 解耦**：阅览模式需要只读但保留平移缩放，拆分为两个独立布尔值避免互相干扰
44. **临时看板自动创建**：固定名称"临时看板"，首次使用时自动 `board.create`，无需用户手动管理
45. **Carousel 纯 CSS 实现**：`overflow: hidden` + `translateX` + `transition` 实现滑动，无第三方依赖；柱状图入场动画仅在面板进入时触发
