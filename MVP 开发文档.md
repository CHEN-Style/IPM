# MVP 开发文档（Electron + JavaScript｜Windows First）

## 0. MVP 定位

这是一款桌面端（Electron）“工作流侵入式”的个人文件与知识碎片收纳工具，面向律师办案过程：**随手捕获（拖拽/截图/复制）→ 快速确认 → 归档到当前客户项目**，并保留基础留痕。

---

## 1. MVP 开发目标

### 1.1 目标（Windows 优先）

在 **Windows** 上交付可用 MVP，实现：

- 悬浮窗（置顶、可拖动、轻量）
- 项目（客户）上下文（底部标签页切换）
- 拖拽文件 → 确认 → 归档到项目目录
- 剪贴板文本捕获 → 确认 → 保存为知识碎片（写入 JSON 索引）
- 截图捕获（Windows 先做“剪贴板图片捕获模式”）→ 确认 → 归档
- 展开中台（三栏布局）浏览项目资产与基础搜索
- 日志记录（JSONL）

### 1.2 原则

- **本地优先**：所有文件和索引只落本地 userData
- **先落盘**：任何内容先进入项目目录/索引，再考虑自动化（Agent 后置）
- **可追溯**：每个条目至少有：时间、类型、来源、存储路径、备注
- **低摩擦**：悬浮窗操作≤2步完成（确认/取消 + 可选备注）

---

## 2. MVP 范围

### 2.1 In Scope（MVP 必做）

1. **悬浮窗（Compact Window）**
- 永远置顶（Windows）
- 可拖动（无边框/自定义拖动区域）
- 中间：拖拽投放区域 + 状态提示文本
- 下方：项目 Tabs（类似网页标签）
- 操作：确认 / 取消
- 输入：备注输入框（可选）
1. **项目与目录结构**
- 创建项目=客户根目录
- 默认子目录：`inbox/` `docs/` `screenshots/` `snippets/`
- 当前激活项目决定归档目标
1. **拖拽归档**
- 拖文件到悬浮窗 → 预览文件名列表 → 确认 → copy 到 `docs/`（或 inbox/，MVP 固定一个也行）
1. **剪贴板文本 → 知识碎片**
- 轮询监听剪贴板文本变化
- 检测到新文本时，悬浮窗提示“检测到可保存内容”
- 用户确认后写入项目 `meta/items.json`（type=snippet）
1. **截图（Windows 先完成可用闭环）**
- MVP 策略：**捕获剪贴板图片**
    - 用户用 Win+Shift+S 截图 → 图片进入剪贴板
    - App 检测到剪贴板图片 → 悬浮窗提示确认 → 保存为 PNG 到 `screenshots/` + 写索引
- 全局快捷键：Windows 可以先做“开启/关闭截图捕获模式”或“唤起悬浮窗并提示截图”（避免难调系统 Snipping Tool 调用）
1. **中台（Expanded Window）**
- 三栏布局（左项目/中列表/右详情）
- 创建项目，新建文件夹，上传文档，删除文档，移动文档
- 列表/树结构展示 items（文件/截图/碎片）
- 详情展示元数据（路径、时间、备注）+ 预览（截图/文本）
- 基础搜索：文件名/备注/碎片内容关键词匹配
1. **索引与日志**
- 元数据：JSON 索引（每项目一份）
- 日志：JSONL 追加写（审计/调试）

---

### 2.2 Out of Scope（MVP 不做）

- SQLite / 数据库
- 复杂 Agent 自动分类（只预留接口）
- OCR、PDF 解析、语音转写
- 自动识别来源 App/窗口标题/URL
- 自研全屏框选截图遮罩层（后续）

---

## 3. Windows First → macOS 后续优化清单

### 3.1 Windows MVP 先保证

- 悬浮窗置顶、拖拽归档、剪贴板文本/图片捕获闭环
- 文件与索引的安全写入
- 中台浏览与搜索

### 3.2 macOS 后续优化（第二阶段做）

- **截图体验增强**：支持快捷键触发 `screencapture -i -c`（写入剪贴板）→ 自动捕获
- 权限提示与引导：屏幕录制权限、辅助功能（若后续需要）
- 窗口表现微调：阴影、透明、置顶层级差异
- 快捷键习惯适配：⌘ vs Ctrl

> 结论：macOS 能适配，但先把 Windows MVP 做成闭环最划算。



#### 0. 约定与范围锁定（开工前 0.5 天）

- 确认落盘根原则：所有内容落本地 userData 管理的“项目根目录”下（或允许用户选择一个“Projects Root”）。

- 确认数据源：中台展示/搜索以 meta/items.json 为权威；目录扫描仅作为后续“重建/修复”能力。

- 确认归档默认目录：拖拽文件默认进 docs/（或 inbox/，二选一先固定）。

- 确认命名策略：统一用 YYYYMMDD-HHMMSS-xxx（或 uuid）避免冲突。

------

#### 1. 工程骨架与进程通信（第 1-2 天）

- 初始化 Electron 工程（主进程/渲染进程/预加载）。

- 建立 IPC 分层：project/*、items/*、fs/*、log/*。

- 搭建基础 UI 外壳：至少能打开 中台窗口（Expanded Window）。

交付验收：能启动应用并进入中台空壳页面；IPC 能跑通一个 “ping”。

------

#### 2. 项目模型与目录结构（第 3-4 天）

- 项目创建：生成项目目录与子目录：inbox/ docs/ screenshots/ snippets/ meta/。

- 项目列表与当前项目状态：中台左栏展示项目，可切换当前项目。

- 持久化项目配置：记录项目 id、名称、路径、创建时间、最近打开时间等（存 userData 下的 app 配置文件）。

交付验收：创建项目后目录结构正确；重启后项目列表仍在；可切换当前项目。

------

#### 3. 索引与日志基础（第 5-6 天）

- 定义并实现 meta/items.json（包含 schemaVersion、items[]）。

- 实现“安全写入”策略：先写临时文件再原子替换（避免损坏）。

- 实现 meta/log.jsonl 追加写（记录关键事件与错误）。

交付验收：对 items 的增删改能正确落盘；异常不会导致 items.json 变成半截文件；日志可持续追加。

------

#### 4. 中台三栏（主功能）第一版：浏览与详情（第 7-10 天）

- 三栏布局落地：左项目 / 中 items 列表 / 右详情。

- 列表来源：读取当前项目 meta/items.json。

- 详情面板：展示元数据（路径、时间、备注、类型）；支持预览：

- snippet：文本预览

- screenshot：图片预览

- file：至少显示文件名/路径（预览可后置）

交付验收：切换项目后列表与详情随之切换；能稳定渲染 100+ items。

------

#### 5. 中台基础操作：文件类资产管理（第 11-14 天）

- 上传文档：选择文件 → copy 到 docs/ → 写 item → 写日志。

- 删除/移动：对 docs/ 和 screenshots/ 的文件执行删除/移动，并同步更新 items.json。

- 新建文件夹：至少支持在项目根或 docs/ 下新建（按你们实际需要定位置）。

交付验收：中台操作能真实改变磁盘文件且索引同步；重启后仍一致。

------

#### 6. 中台搜索（第 15-16 天）

- 关键词搜索（当前项目）：匹配 filename/path/note/snippetContent。

- 结果在中栏列表即时过滤；右侧详情仍可查看。

交付验收：搜索命中准确；大列表不卡顿（先保证可用，再优化）。

------

#### 7. 悬浮窗（第 17-19 天）

- Compact Window：置顶、无边框、可拖动、底部项目 Tabs、备注输入、确认/取消。

- 与中台共享“当前项目”状态（或至少能在悬浮窗内切换项目并影响保存目标）。

交付验收：悬浮窗可独立完成“选择项目 + 输入备注 + 确认/取消”的交互闭环。

------

#### 8. 捕获能力接入（第 20-25 天）

- 拖拽文件到悬浮窗：预览 → 确认 → copy 落 docs/ → 写 item/log。

- 剪贴板文本轮询：检测变化 → 悬浮窗提示 → 确认写入 item（snippet）。

- 剪贴板图片检测（Windows）：检测图片 → 悬浮窗提示 → 确认保存 PNG 到 screenshots/ → 写 item/log。

- 视情况加一个全局快捷键：开启/关闭“截图捕获模式”或唤起悬浮窗。

交付验收：三种捕获均能在磁盘 + items.json + log.jsonl 形成一致闭环。

------

#### 9. 收尾与质量（第 26-30 天）

- 异常与边界：权限、重名、超长路径、索引损坏提示与最小恢复策略。

- 打包与安装：产出 Windows 可分发包；写简短使用说明与项目目录说明。

- 最小可用体验优化：提示文案、加载状态、失败可重试。

交付验收：干净机器上安装可用；核心流程无阻塞；数据不会“丢/乱”。

---

## 10. 知识碎片（Clipboard Snippet）方案讨论记录（MVP 阶段先锁定最小实现）

### 10.1 我们的顾虑与思考

- **碎片数量激增**：如果“单次剪贴板=一个文件”，长期使用会导致 `snippets/` 下文件数量变多，影响人工管理与备份体验。
- **可读性 vs 可索引性**：
  - 直接把全文写进 `meta/items.json`：对索引/展示友好，但 `items.json` 会快速膨胀、写入风险更高。
  - 内容落为独立文本文件：用户可直接查看/编辑，但可能带来文件数量管理问题。
- **不同来源的知识碎片归档问题**：剪贴板导入的短文本 vs 用户自己整理并上传的文件型知识（后续可能接入）。

### 10.2 MVP 阶段决策（先不开发高级方案）

为降低实现复杂度，MVP 阶段先锁定最小闭环：

- **存储形式**：`items.json + txt 文件`
  - **单次剪贴板文本 = 1 个 txt 文件 + 1 条 items.json 记录**
  - 暂不做“按日/月聚合到一个 md/txt 中”的方案
- **归档范围**：
  - MVP 仅实现 **剪贴板文本导入** 的知识碎片
  - 暂不支持“用户自己上传文件作为知识碎片”（后续迭代再讨论目录与索引策略）

### 10.3 MVP 交互约定（悬浮窗）

- **触发**：悬浮窗模式下，只要用户执行复制（剪贴板文本变化），悬浮窗中心区域提示“是否上传该段内容为知识碎片”。
- **确认**：用户点击后立即保存（写入 txt + 写入 items.json 索引）。
- **优先级**：如果在等待确认期间用户拖拽文件到悬浮窗，则 **覆盖并清空** 当前剪贴板知识碎片确认内容（以文件上传交互为准）。


----
现在我们需要在@desktop 文件夹下新建一个Agent文件夹，其中会包含所有agent相关的逻辑，文件，代码，采用LongChain架构来构建。
我们先要实现的是一个文件自动分类到相应文件夹的Agent，这个Agent的职责是：每当用户通过悬浮窗上传文件时（即文件上传到了temp文件夹下后），就会触发这个agent，使得agent读取这个文件的文件名和后缀（不读取其他的任何内容）然后Agent浏览这个项目的@desktop/userfile/projects/客户A/meta 中的@desktop/userfile/projects/客户A/meta/structure.json 里的文件夹结构和文件夹描述，找到最适合这个文件应该归属的文件夹。
但需要注意的是，这个Agent实际上并没有操作文件的能力，它只是接收所有信息，判断文件归属于哪个文件夹，然后输出结论，系统会接收这个结论，然后在这个项目的@desktop/userfile/projects/客户A/meta 文件夹下，会有一个ai-storage.json，这个文件收录了AI会把x文件放在y文件夹下的标准化结论。
类似git的暂存区效果，系统也不会马上直接根据AI的建议，读取ai-storage.json然后移动文件，而是先读取ai-storage.json，根据其中的数据在中台中的各个文件夹下会显示幽灵文件（有点像ide中的accept效果）只有用户点击确认移动，才会真的移动到这个位置（用户也能够一键全部接受移动或放弃）。

请你先消化总结上面的内容，然后列出todo，我们一步步开发

我对需求的消化总结（按链路拆解）
触发时机：用户通过悬浮窗上传文件 → 文件先落到项目的 temp/（系统现有逻辑）→ 立刻触发自动分类 Agent
Agent 的输入：
仅文件名 + 后缀（不读取文件内容）
当前项目的 meta/structure.json（业务目录结构 + 每个文件夹的 description）
Agent 的输出：判断“这个文件最适合归属哪个文件夹”，输出标准化结论（例如：sourceRelPath、suggestedFolderRelPath、置信度/理由/时间戳…）
关键边界：Agent 不具备文件操作能力，只做决策
系统落盘（暂存区）：
在项目 meta/ 下新增 ai-storage.json
记录“AI 建议把 x 文件放到 y 文件夹”的 pending 结论（类似 git 暂存区）
中台呈现（幽灵文件）：
中台读取 ai-storage.json，在相应文件夹下显示“幽灵文件”
用户可以：单个确认移动 / 单个放弃 / 一键全部接受 / 一键全部放弃
最终执行：只有用户确认后，系统才真正移动文件；并更新 ai-storage.json 状态
TODO（按依赖顺序，我们逐条做）
我已经把 TODO 写进任务列表，核心项如下（精简版）：
定义 Agent 输入/输出协议（只读信息 + 标准化决策输出）【进行中】
新增 desktop/Agent/ 目录并搭 LangChain 架构骨架（runner/prompts/tools/schemas/storage/README）
实现 structure.json 读取与候选文件夹构建（排除 system 目录）
做一个 V1 分类器（先规则/关键词启发式，封装成 LangChain Runnable，后续可替换 LLM）
在悬浮窗上传到 temp 后触发 Agent（主进程接住 savedRelPath）
设计并写入 meta/ai-storage.json（幂等去重、状态机 pending/accepted/rejected）
中台展示幽灵文件（基于 ai-storage.json）
实现接受/拒绝/批量操作（接受才 move 文件）
日志与安全护栏（禁止建议 system 目录、写 log.jsonl）